function compareStrings(str1, str2) {
  return str1 === str2 ? "strings are equal" : "strings are not equal";
}
console.log(compareStrings("hello", "world"));
console.log(compareStrings("hello", "hello"));
